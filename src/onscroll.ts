const onscroll = {
 run: () => {
  /* window.addEventListener("scroll", () => {
   const nav = document.getElementById("Nav")
   if (window.scrollY > 20) {
    nav?.classList.add("fixed"); nav?.classList.remove("absolute")
   } else {
    nav?.classList.remove("fixed"); nav?.classList.add("absolute")
   }
  }) */
 }
}

export default onscroll
